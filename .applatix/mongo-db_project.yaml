---
type: project
name: Mongo-db
description: This is the workflow to deploy Mongo container, insert MLB data then deploy Node.js container with a web application to read MLB data.
actions:
  -
    name: run
    template: Deploy MongoDB
categories: ["database", "promoted"]
publish:
  branches:
    - master
labels:
    tags: "[\"database\"]"
assets:
    icon: "/mongo-data/icon.png"
    overview: "/mongo-data/overview.md"
    detail: "/mongo-data/detail.md"
