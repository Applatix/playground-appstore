---
type: project
name: Nginx
description: Web servier, reverse proxy, load balance and HTTP cache.
actions:
  -
    name: test
    template: nginx server test
categories: ["reverse proxy"]
publish:
  branches:
    - master
labels:
    tags: "[\"network\"]"
assets:
    icon: "/nginx/nginx.png"
    detail: "/nginx/README.md"
